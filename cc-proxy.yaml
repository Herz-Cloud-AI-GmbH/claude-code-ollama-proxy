schema_version: 1
default_alias: sonnet
user_config_path: "~/.config/cc-proxy/cc-proxy.user.yaml"

thinking_capable_models:
  - qwen3:14b
  - qwen3:8b
  - qwen3:4b

tool_calling_capable_models: []

verbose_tool_logging: false

tool_call_streaming_enabled: false

aliases:
  sonnet:
    promise:
      tool_calling: planned
      notes: "Tool calling depends on model capability and runtime config."
  haiku:
    promise:
      tool_calling: planned
      notes: "Tool calling depends on model capability and runtime config."
  opus:
    promise:
      tool_calling: planned
      notes: "Tool calling depends on model capability and runtime config."

# Disable lifecycle scripts (preinstall / postinstall / prepare) to prevent
# supply-chain attacks (Shai Hulud). Individual packages that legitimately need
# a postinstall step (e.g. esbuild binary download) must be explicitly rebuilt
# via `npm rebuild <pkg>` in the Makefile.
ignore-scripts=true

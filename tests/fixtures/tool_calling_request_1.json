{
  "model": "qwen3:4b",
  "max_tokens": 128,
  "temperature": 0,
  "stream": false,
  "tools": [
    {
      "name": "get_weather",
      "description": "Get the current weather for a location.",
      "input_schema": {
        "type": "object",
        "properties": {
          "location": {
            "type": "string",
            "description": "City name, e.g. Berlin"
          }
        },
        "required": ["location"],
        "additionalProperties": false
      }
    }
  ],
  "system": [
    {
      "type": "text",
      "text": "You must call the tool. Do not answer in natural language."
    }
  ],
  "messages": [
    {
      "role": "user",
      "content": [
        {
          "type": "text",
          "text": "Call get_weather with location=Berlin. Return only the tool call."
        }
      ]
    }
  ]
}
